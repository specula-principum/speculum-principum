default_finish: Preview ready for operator review.
steps:
  - description: Scan parse root for candidate documents
    tool: list_parse_candidates
    arguments:
      root:
        context: parse_root
  - description: Generate Markdown preview for selected source
    tool: preview_parse_document
    arguments:
      source:
        context: source_path
